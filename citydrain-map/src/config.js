const config = {
  // Railway 백엔드 주소를 직접 사용
  API_URL: 'https://backendflask-production-f4c6.up.railway.app',
  // MapTiler API 키 추가
  MAPTILER_KEY: 'https://basemaps.cartocdn.com/gl/positron-gl-style/style.json'  // 실제 API 키로 교체 필요
};

export default config; 